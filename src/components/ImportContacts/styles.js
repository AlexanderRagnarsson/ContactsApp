import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    alignContent: 'center',
    paddingBottom: 3,
  },
  text: {
    fontSize: 18,
    paddingRight: 10,
    paddingBottom: 0,
  },
});
