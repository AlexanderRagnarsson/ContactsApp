import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  view: {
    alignItems: 'center',
    alignContent: 'center',
    justifyContent: 'center',
  },
  text: {
    marginBottom: 5,
  },
  image: {
    marginVertical: 12,
    width: 150,
    height: 150,
    borderRadius: 150 / 2,
  },
  imageView: {
    flexDirection: 'row',
    justifyContent: 'center',
  },
});
