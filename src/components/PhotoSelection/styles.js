import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  icon: {
    fontSize: 60,
    marginTop: 20,
    marginBottom: 20,
  },
  image: {
    width: 200,
    height: 100,
  },
});
